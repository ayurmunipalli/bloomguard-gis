# Python — Stage-2 transformer ONLY (PLAN.md: Python only for the DL step).
# The data/spatial/Stage-1 pipeline is R (renv.lock).

# Core DL
torch>=2.0
numpy>=1.23
pandas>=1.5
pyarrow>=10.0        # read model_dataset.parquet / per-cell sequences
scikit-learn>=1.2    # metrics parity with R (PR-AUC, ROC-AUC, recall)
matplotlib>=3.6      # figures
pyyaml>=6.0          # read config.yaml

# Optional pulls (only if a dataset is pulled from Python rather than R)
# cdsapi>=0.6         # ERA5 via Copernicus CDS
